name: Format Debug

on:
  push:
    branches:
      - feat/build-artifact-runtime-setup

permissions:
  contents: read

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
      - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2
        with:
          bun-version: "1.4.0"
      - run: bun install --frozen-lockfile
      - name: Show formatter diff
        run: |
          bunx oxfmt --write .github/workflows/build-artifact.yml
          git diff -- .github/workflows/build-artifact.yml
